# Day 13 – Encapsulation

Implementation of a simple `Account` class with private fields.

**Concepts practiced:**
- `private` variables
- Getter and setter pattern
- Data protection (Encapsulation)

Features:
- `balance` is private
- `getBalance()`
- `deposit(amount)`
