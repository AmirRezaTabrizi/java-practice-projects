# 🧮 Day 8 – MaxFunction

This simple Java program demonstrates how to find the **maximum of two numbers** using a custom function.

---

## 🧠 Key Concepts

- **Methods with parameters and return values**  
- **Ternary operator (`? :`)** for conditional selection  
- **Basic input/output** with `Scanner`  

---

## 📄 Description

The program defines a method `max(int x, int y)` that receives two integers and returns the larger one.

It then prompts the user to enter two numbers, calls the `max()` function, and prints the result.

---

## 📝 Example Output

=== Day 8 – MaxFunction ===
Enter first number: 7
Enter second number: 12
The maximum is: 12
