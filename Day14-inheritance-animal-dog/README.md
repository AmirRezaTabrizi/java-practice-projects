# Day 14 – Inheritance

Basic inheritance example in Java.

**Concepts practiced:**
- Parent class `Animal`
- Child class `Dog`
- Method overriding

`Dog` overrides `speak()` to provide specific behavior.
